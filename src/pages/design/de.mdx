import { getStaticProperties } from "@/ions/ssr/get-properties";
import { MDLayout } from "@/organisms/layout/md";
import { DesignSystem } from "@/templates/design/components";

# Design System

<DesignSystem/>

export default function Page({ children }) {
  return (
    <MDLayout noIndex seo={{title: "Design System"}}>
      {children}
    </MDLayout>
  );
}

export async function getStaticProps(context) {
  return getStaticProperties(context);
}

